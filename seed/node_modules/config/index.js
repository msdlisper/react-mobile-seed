/**
 * @file --
 * @author zhangpeng
 */

const path = require('path');
const seedDirName = '/seed/';

const config = {
    root: path.resolve(__dirname, '../../../'),
    path: {}
};
config.path.build = config.root + seedDirName + 'node_modules/build/';
config.path.befe = config.root + '/src/node_modules/@befe/';
config.path.templateDevHtml = config.path.build + 'template/template-dev.html';
config.path.templateProdHtml = config.path.build + 'template/template-prod.html';
config.path.destTargetHtml = config.root + '/dest/prod/';
config.path.prod = config.root + '/dest/prod/';
config.path.dllDev = config.root + '/dest/dll-dev/';
config.path.dllProd = config.root + '/dest/dll-prod/';
module.exports = config;
